안녕하세요,

공용 호스팅 서비스팀 담당자님께 보낼 차단 해제(appeal) 요청 초안입니다.

요약
- 사건: 우리 서비스에서 실행 중인 자동화 스크립트가 외부 메일(SMTP/IMAP) 계정을 대상으로 짧은 간격으로 접속/요청을 반복하여, Gmail 쪽에서 비정상 트래픽으로 간주되고 결과적으로 호스팅 IP가 블랙리스트에 등록되었습니다.
- 영향: 서비스가 차단되어 고객(우리 서버를 사용하는 다수의 길드)에게 피해가 발생했습니다.

원인 및 조치
1) 원인: 내부 코드가 메일 수신을 폴링(polling) 방식으로 구현하여 짧은 주기(초당 다수 요청)로 IMAP 서버에 접속해 패턴 상 루프성 에러 트래픽을 유발했습니다.
2) 즉시 적용한 완화책:
   - IMAP 폴링 간격을 5초에서 30초로 늘려 폴링 빈도를 6배 완화했습니다.
   - 폴링당 처리 메시지 수를 제한(최대 5건)하여 한 번에 많은 메일을 처리하지 않도록 했습니다.
   - SMTP 전송은 분당 기본 20건으로 레이트 제한을 추가해 전송 폭주 위험을 낮췄습니다.
   - 반복 안내(같은 인증 코드에 대한 알림)는 내부 쿨다운을 적용해 중복 알림을 막았습니다.
3) 추가 권장 조치 (진행 예정):
   - 현재 IMAP 폴링 대신 Gmail API (Pub/Sub) 또는 상용 SMS 게이트웨이로 전환 검토.
   - 사용 중인 SMTP/IMAP 자격증명 교체 및 2단계 인증 활성화 권고.

복구 요청
- 해당 IP(또는 계정)에 대한 블랙리스트/차단을 해제해 주시길 정중히 요청드립니다. 문제는 내부 로직 변경으로 완화 조치를 이미 적용했으며, 재발 방지 계획(추가 전환 작업 포함)을 수립 중입니다.

첨부
- 적용된 변경사항 요약 (IMAP 폴링 간격/폴링당 처리 제한/SMTP 레이트 제한)
- 재발 방지 계획(단계별)

감사합니다.
